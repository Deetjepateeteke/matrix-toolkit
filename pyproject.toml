[project]
name = "matrix-toolkit"
version = "0.1.1"
description = "A Python add-on that introduces a 2-dimensional matrix datatype."
authors = [
    { name = "Deetjepateeteke" }
]
license = { text = "MIT" }
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy"
]

[project.optional-dependencies]
dev = [
    "pytest",
    "flake8"
]